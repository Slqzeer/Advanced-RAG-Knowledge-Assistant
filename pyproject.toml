[project]
name = "advanced-rag-knowledge-assistant"
version = "0.1.0"
description = "Assistant documentaire technique fondé sur une architecture RAG avancée."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116,<1",
    "openai>=2,<3",
    "pydantic-settings>=2.10,<3",
    "qdrant-client>=1.15,<2",
    "uvicorn[standard]>=0.35,<1",
]

[dependency-groups]
dev = [
    "mypy>=1.17,<2",
    "pre-commit>=4.3,<5",
    "pytest>=8.4,<9",
    "ruff>=0.16,<1",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
# Plans and specs are prose; their code blocks are sketches, not source.
extend-exclude = ["docs"]
target-version = "py312"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "SIM", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
addopts = "-ra"
markers = ["requires_qdrant: needs a running Qdrant (docker compose up -d qdrant)"]
pythonpath = ["."]
testpaths = ["tests"]
